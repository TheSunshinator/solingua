main -> {
    a -> 10
        is value, type Integer,
    b -> 3
        is value, type Integer,

    printLine(a + b)
    printLine(a - b)
    printLine(a * b)
    printLine(a / b)
    printLine(2 + 3 * 4)
    printLine(a * 2 + b)
}
    is function, returns nothing,
